import React, { useState } from "react";
import {adminLinks} from "../routers/links";

const Eventos = () => {

    return(
        <div>
            <h1>Eventos</h1>
        </div>
    );
}

export default Eventos;
